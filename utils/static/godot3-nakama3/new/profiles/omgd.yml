omgd:
  game:
    targets:
      - build-service: build-windows
      - build-service: build-mac
      - build-service: build-web
      - build-service: build-x11
  gcp:
    project: your-project-name
    zone: us-east1-c
  name: your-game-name
  servers:
    services:
    - central
    - web
